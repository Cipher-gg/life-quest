# Copy this file to `.env` and fill in your Cloudflare Tunnel token.
# `.env` is gitignored — never commit your real token.
#
# Get the token from:
#   Cloudflare Zero Trust dashboard -> Networks -> Tunnels
#   -> Create a tunnel -> Cloudflared -> copy the token from the install command.

TUNNEL_TOKEN=
